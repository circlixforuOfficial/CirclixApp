apiVersion: v1
kind: Service
metadata:
  name: user-service
spec:
  selector:
    app: user-service
  ports:
    - protocol: TCP
      port: 8080          # ClusterIP port
      targetPort: 8080    # Container port
  type: ClusterIP        # Must be ClusterIP for Ingress
